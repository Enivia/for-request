import RequestClass from './request';
import { Request } from './interface';

const requestEntity = new RequestClass();
const request = requestEntity.request;

export default request;
export { Request };
