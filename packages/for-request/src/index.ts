import RequestClass from './request';
import { Request } from './interface';

const requestClass = new RequestClass();
const request = requestClass.request;

export default request;
export { Request };
